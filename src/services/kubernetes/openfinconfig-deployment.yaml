apiVersion: apps/v1
kind: Deployment
metadata:
  name: openfinconfig-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: openfinconfig
  template:
    metadata:
      labels:
        component: openfinconfig
    spec:
      volumes:
        - name: google-cloud-key
          secret:
            secretName: nlp-key
      containers:
        - name: openfinconfig
          image: reactivetradercloud/openfinconfig:v1.3.2-453-g07902e79
          ports:
            - containerPort: 8080
